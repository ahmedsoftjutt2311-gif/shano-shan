# SHANO SHAN FINAL FIX — 2026-09-25

## Exact fixes in this build

1. Customer inner pages no longer show the intro overlay. The intro is only shown on the home route and has a 9-second safety timeout plus video-error fallback.
2. Shop page receives a dedicated visible luxury layout instead of a near-black/blank appearance. Product cards and toolbar have clear contrast.
3. Home and shop product cards use **ADD TO CART** as a real button. Admin Website Controls includes an **Add to Cart** switch.
4. Admin Website Controls retains its existing design and now controls the Add to Cart feature too.
5. Maintenance Mode is functional: when enabled in Admin > Website Controls, the customer site shows a branded **Website Under Maintenance / MAINTENANCE MODE** page.
6. Admin password recovery uses an account recovery maths challenge and no current password is required. Recovery table creation is defensive in the Worker so the recovery endpoint does not fail simply because migration 0008 has not yet been applied.
7. Customer password recovery continues to use a new maths question each recovery request.
8. Existing notification delete endpoints/UI remain enabled.
9. Existing D1, Worker/Hono, Cloudinary, authentication and production architecture are preserved. No database reset/recreation is included.

## Deployment

Deploy the updated `customer`, `admin`, and `worker` projects. If migration 0008 has not been applied remotely, apply the normal migrations; do not reset the D1 database.
